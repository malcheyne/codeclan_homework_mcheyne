Git + Github

-Version Control Software
        Can go back to saved file's "revert"

-Git repository if same as folder
        Github repository -> R directory -> folder

-Initialize a repository links to Github, makes a folder

-Staging -> file changed but not uploaded "a change to the file"

-Commit -> bookmarks saves, done at points when your happy with the change

-Push -> sends to Github, can be seen by everyone

-Reverting -> going back to a parviose Commit. ie now not working, go back a
        day or two to when it was.

-Copy files -> makes a Branch > lets you add a feature > then merges back
        into main Branch

-Main Branch is the new way to write Master Branch > old style may see in 
